# Pass-through for Android TWA verification — must come before SPA catch-all
/.well-known/assetlinks.json  /.well-known/assetlinks.json  200!

# Pass-through for self-update files — must come before SPA catch-all
/update.json                  /update.json                  200!
/downloads/*                  /downloads/:splat             200!

# SPA catch-all — serves index.html for all other routes
/*  /index.html  200
